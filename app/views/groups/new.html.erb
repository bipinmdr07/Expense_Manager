<%= form_for @group do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :users %>
  <%= f.select :users, multiple: true, include_blank: true %>

  <%= f.submit 'Create Group' %>
<% end %>